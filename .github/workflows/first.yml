name: 'Terraform GitHub Actions'
on:
  - pull_request
  - push
jobs:
  runshellcommands:
    name: 'Terraform'
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo "hello"
      - name: multilinestring
         run: |
         ls -a
         pwd
     name: pythoncommand
     run: |
     import platform
     print (platform.processor())
    shell: python    

    

        
        
          
          
        
      