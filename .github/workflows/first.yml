name: 'Terraform GitHub Actions'
on:
  - push
jobs:
  run-shell-commands:
    runs-on: Red Hat
    steps:
      - name: echo string
        run: echo "hello"
      - name: multilinestring
        run: |
         ls -a
         pwd
     name: python command
     run: |
     import platform
     print (platform.processor())
    shell: python 
run-on-windows-commands:
  runs-on: ["run-shell-commands"]
  steps: 
    -name: Directory
     run: pwd
     shell: bash


    

        
        
          
          
        
      