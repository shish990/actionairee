name: 'Terraform GitHub Actions'
on:
  - push
jobs:
  run-shell-commands:
    runs-on: Red Hat
    steps:
      - name: echo string
        run: echo "hello"
      - name: multilinestring
        run: |
         ls -a
         pwd
     name: pythoncommand
     run: |
     import platform
     print (platform.processor())
    shell: python    

    

        
        
          
          
        
      